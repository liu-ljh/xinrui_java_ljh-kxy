<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.xinrui.mapper.EmrExClinicalItemMapper">
    <select id="selectEmrExClinicalItem" resultType="org.xinrui.entity.EmrExClinicalItem">
        SELECT
            exam.OID AS oid,
            rpt.Report_ID AS reportId,
            exam.Modality AS modality,
            rpt.Positive AS positive
        FROM T_Pacs_Exam exam
                 LEFT JOIN T_Pacs_Exam_Report rpt ON exam.OID = rpt.Exam_OID
        WHERE exam.Study_Time BETWEEN #{startTime} AND #{endTime}
    </select>
</mapper>
